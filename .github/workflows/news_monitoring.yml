name: GDELT Monitoring

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gdelt-monitoring
  cancel-in-progress: true

jobs:
  run-gdelt:
    runs-on: ubuntu-latest

    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync latest main
        run: |
          git pull origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install packages
        run: pip install -r requirements.txt

      - name: Run GDELT monitoring
        run: python gdelt_monitoring.py      
        
      - name: Run CrewAI Monitoring
        run: python crewai_monitoring.py

      - name: Commit results
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          git add gdelt_results.json gdelt_monitoring.csv crewai_results.json
          git commit -m "Update GDELT and CrewAI monitoring results" || echo "No changes to commit"
          git push origin main
